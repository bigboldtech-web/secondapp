{
  "name": "@second-app/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed": "tsx seed.ts",
    "db:setup-fts": "tsx scripts/setup-fts.ts",
    "db:build-search-vocab": "tsx scripts/build-search-vocab.ts",
    "db:expire-listings": "tsx scripts/expire-listings.ts",
    "db:trigger-alerts": "tsx scripts/trigger-deal-alerts.ts",
    "db:promote-categories": "tsx scripts/promote-categories.ts",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "tsx": "^4.0.0"
  }
}
